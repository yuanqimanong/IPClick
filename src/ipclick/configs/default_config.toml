# ================ SDK配置 [CLIENT] ================
# -------------- 基础配置 [GENERAL] --------------
[GENERAL]
# 程序运行模式: "standalone" (单机) | "cluster" (多机器/集群)
# ⚠️ 尚未实现：集群模式待 [CLUSTER] 一并支持，当前恒为单机
mode = "standalone"
# 调试模式 (开启后强制 DEBUG 级别日志，覆盖 [LOG].level)
debug = false

# ------- 代理配置 [PROXY] -------
# 供 downloader.get(url, proxy=True) 使用。默认留空表示"未配置代理"，
# 此时 proxy=True 会打一条警告并直连——不要在这里预置某台机器的本地代理地址，
# 否则所有用户的 proxy=True 都会指向他们自己的 127.0.0.1:7890（可能是别的服务）。
[PROXY]
scheme = 'http'
host = ''
port = 0
# 三方隧道代理
auth_key = ''
auth_password = ''
channel_name = ''
session_ttl = ''
country_code = ''
tunnel_server = ''

# ------- 集群配置 [CLUSTER] -------
# ⚠️ 尚未实现：本节配置当前没有任何消费方，修改不会生效（详见 README「尚未实现 / 已知限制」）
[CLUSTER]
# 负载均衡策略 (round_robin/random/weight)
load_balancer = "round_robin"
nodes = [
    # 格式：[id, address, region(可选), zone(可选), weight(可选), tags(可选)]
    { id = "node-101", address = "192.168.1.101:8080", region = "us-east-1", zone = "1a", weight = 100 },
]
db_uri = "mysql://..."

# ================ 服务端配置 [SERVER] ================
[SERVER]
# 监听地址
host = "[::]"
# 监听端口
port = 9527
# 最大线程数（每个进行中的请求占用一个线程）
max_workers = 100

# ================ 安全配置 [SECURITY] ================
# 服务端会代替调用方请求任意 URL。若监听在公网或不可信网络，
# 请务必打开 block_private_networks，否则本服务可被当作内网跳板（SSRF）。
[SECURITY]
# 调用方必须携带的鉴权令牌。留空 = 不鉴权（服务端启动时会打显著告警）。
#
# 强烈建议用环境变量 IPCLICK_AUTH_TOKEN 提供，不要把密钥写进配置文件；
# 环境变量优先于此处。也可以写成列表以便轮换期间新旧令牌并存：
#   auth_token = ["新令牌", "旧令牌"]
#
# 注意：鉴权解决的是"谁能用"，SSRF 防护解决的是"能打到哪儿"，两者不能互相替代。
auth_token = ""
# 允许的 URL 协议
allowed_schemes = ["http", "https"]
# 拦截云厂商实例元数据地址（169.254.169.254 等），无正当代理用途，建议保持开启
block_metadata_endpoints = true
# 拦截回环 / 私网 / 保留地址。默认关闭以兼容"本机代理本机服务"的用法，
# 部署到不可信网络时应改为 true
block_private_networks = false
# 即便开启上面的拦截也放行的主机名或 IP
allowlist = []

# ------- 下载器配置 [DOWNLOADER] -------
# 这些是"请求未显式传参时"的默认值。单次请求传的 timeout / max_retries /
# retry_backoff 优先级更高。
[DOWNLOADER]
# 连接超时（秒）
connect_timeout = 10
# 下载超时（秒）
download_timeout = 300
# 是否读取环境变量里的代理设置（HTTP_PROXY / ALL_PROXY）。
# 默认关闭：代理应由调用方显式指定，而不是取决于服务端所在机器的环境
trust_env = false
# 失败重试策略
[DOWNLOADER.retry]
max_attempts = 3                     # 最大重试次数（0 = 不重试）
backoff_exponent = 2.0               # 退避指数（等待 = initial_backoff * exponent^已重试次数）
initial_backoff = 1                  # 初始等待时间（秒）
max_backoff = 30                     # 单次等待上限（秒，硬上限 300）
retry_codes = [429, 500, 502, 503, 504]  # 触发重试的状态码；连接层异常总是会重试
# 连接池
[DOWNLOADER.concurrency]
max_connections = 100           # 连接池总上限
max_keepalive_connections = 20  # 保持长连接的上限

# 说明：限速（rate_limit）、分块下载（chunk）、临时存储（storage）依赖真正的
# 流式传输通路，当前的 protobuf 协议是整个响应体一条消息，无法支持。
# 这些配置项已从默认配置移除，待流式下载实现后再引入，避免"配了却不生效"。

# ------- 浏览器渲染配置 [BROWSER] -------
# ⚠️ 尚未实现：本节配置当前没有任何消费方，修改不会生效（详见 README「尚未实现 / 已知限制」）
[BROWSER]
# 启用浏览器渲染引擎
enabled = true
# 无头模式 (无GUI界面)
headless = true
# 用户代理 (伪装浏览器标识)
user_agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/115.0.0.0 Safari/537.36"
# 视窗尺寸
viewport = { width = 1920, height = 1080 }
# 插件配置
[BROWSER.plugins]
# 插件安装目录
install_dir = "/path/to/browser/plugins"
# 启用插件列表
enabled = [
    "adblock.crx",
    "web-assistant.zip"
]
# 禁用插件列表
disabled = ["flash_player.dll"]
# 代理网关 (覆盖系统代理)
[BROWSER.proxy]
gateway = "http://127.0.0.1:8080"  # 指向本地代理服务
bypass_list = ["*.internal.com"]    # 代理排除列表
# 渲染超时控制 (秒)
[BROWSER.timeout]
page_load = 30      # 页面加载超时
script_exec = 60    # JS执行超时
render = 20         # 渲染完成检测超时
# 缓存管理
[BROWSER.cache]
max_size = 500      # 缓存上限(MB)
persist = true      # 持久化缓存
path = "/data/browser_cache"
# 安全沙箱配置
[BROWSER.sandbox]
enable = true
level = "strict"    # strict/moderate/off



# ================ 监控配置 [MONITOR] ================
[MONITOR]
# 是否注册 grpc.health.v1 健康检查服务（免鉴权，供 K8s 探针 / 服务网格使用）。
# 关闭后探活会收到 UNIMPLEMENTED。
health_check = true

# ================ 日志配置 [LOG] ================
[LOG]
# 日志级别 (debug/info/warn/error)
level = "info"
# 日志格式
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
# 日志输出位置 (stdout 或文件路径)
output = "stdout"
# 日志轮转配置 (当 output 为文件时生效)
[LOG.rotation]
max_size = 100   # 单文件最大(MB)
max_backups = 5  # 保留历史文件数

